class Program
{
    static void Main(string[] args)
    {
        // Point d'entrée du programme, appelez vos fonctions ici.
    }


    // TODO: Créez des fonctions et documentez les (entrez /// pour générer la structure de documentation de la fonction).

    /*
        Quelques notions à utiliser

        Hauteur de la fenêtre console en nombre de lignes : Console.WindowHeight
        exemple :
        int sol = Console.WindowHeight - 2;

        Positionner le curseur sur une position spécifique dans la console:
             x : la colonne(de gauche à droite, commence à 0).
             y : la ligne(de haut en bas, commence à 0).
        Console.SetCursorPosition(x, y);

        Configuration pour afficher les emojis
        Console.OutputEncoding = System.Text.Encoding.UTF8;

        Random random = new Random(); // Une seule instance de Random pour éviter les répétitions
        random.Next(posMin, posMax + 1);

        Temps de pause (en millisecondes)
        Thread.Sleep(50);
    
        Effacer la console 
        Console.Clear();
     */



}